# 工作日志 2026-09-15：C2.3 verdict（截断mask进loss，挂起）

> 车型M1@E0P2+sumnorm，10轮 ｜ 对照C2.1：eDOS 0.444/5.2%，phDOS 0.827/0.6%
> 实验（--use_mask）：eDOS 0.452/5.1%，phDOS 0.799/0.6%

## 判决：挂起（声子侧-0.028超噪声线，电子侧+0.008噪声内）

- 分层一致：eDOS原生+0.007/增量+0.001（平），phDOS原生-0.026/增量-0.038（全负）。
- 机制解释：掩膜零bin是弱但有效的边界监督（教模型“谱在哪里结束”）；
  从loss里拿掉等于删掉最简单的正样本，净效应为负。掩膜的正确位置是**评估端**
  （R²cov诚实性，C2b已立规），不是训练端。
- 距离衰减未做：属E9-G1（Design-E分工已定），C2.3到此关闭，不越界。

## 实施教训（NaN两连杀，必读）

1. KL在掩膜bin上是`0*(+inf)=nan`：q=0且logp=-inf（masked softmax）。
   修法：`torch.where(eff, inner, 0)`按元选取丢弃——注意乘法清零不行（nan先传播），
   且where条件须bool张量（float条件直接报错）。
2. tuple 12→14的blast radius：5处解包点（model×2/eval/pilot/ablation）逐个改；
   v1无掩膜文件路径返回None→全1，test_cif同理。
3. C2.1对照（c21）即本轮对照臂，一鱼两吃；C2.3代码合入主线默认关闭。
