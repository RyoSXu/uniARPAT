# 工作日志 2026-09-11：A3c PhononDB覆盖映射收官

## 改了什么
- 新增 `tools/getdata/a3c_phonondb_coverage.py`（MDR serial→legacy `mp-N`→canonical翻译+交集统计，断点续跑）；
- 新增 `tools/getdata/a3c_validate.py`（全本地分布验证：元素/maxfreq/晶系）；
- 落盘（`getdata/raw/`，不进库）：
  `phonondb_a3c_map.jsonl`（9938条）、`phonondb_a3c_summary.json`、
  `phonondb_a3c_validation.json`、`phonondb_a3c_unresolved.json`（96）、
  `phonondb_a3c_outside_effective.json`（2）。

## 结果如何
- 翻译：10,034/10,034 serial与复算1:1；9,938（99.0%）译出唯一canonical，无碰撞；
  96死ID（`mp-867xxx`系占~80%，MP已删库，单查0返回）按census先例挂`unresolved`，进L2候选。
- 交集：有效全集内9,936；与MP有声子集交5,545；真双谱交4,660；
  **增量核心4,391**（有效全集内、MP无声子、有eDOS）→ 真双谱可从18,644扩到~23,035（+23.5%）；
  另885（MP有声子无eDOS）得第二声子视图；2在有效全集外。
- 分布验证（通过）：元素覆盖80 vs 80，top15基本同集合，氧化物占比差0.091；
  maxfreq中位25.01 vs 20.87THz（+19.8%，贴线，A2b需逐材料配对复核是泛函/网格系统差还是成分偏）；
  晶系六类全覆盖，增量略偏低对称（单斜36%+正交30%，立方7% vs 14%）→ A6本就按晶系分层，无阻塞。
- 教训（已修进脚本）：
  1. Delta表（eDOS/phDOS）只按task `identifier`存、无`material_id`列——legacy→canonical只能走summary，本地代替不了ID翻译；
  2. 整批查询被~1%死ID毒死（99/100整批作废），后段切`--singles-only --workers 4`才跑完；
  3. 后台`&`会被工具超时连带杀掉，用断点续跑+分段前台跑。

## 下一步
A2b三极gap（全本地：`mp_phdos_raw.jsonl` + `phonondb_recomputed.jsonl`在交集子集上配对比较，
THz→cm⁻¹×33.35641，mesh/展宽先对齐）→ 定源权重 → A4加工。
96死ID + 2集外走L2结构匹配（A7 L2待议时一并定）。
