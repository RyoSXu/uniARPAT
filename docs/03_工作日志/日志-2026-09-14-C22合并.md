# 工作日志 2026-09-14：C2.2 verdict（守恒缩放，并入推理口径）

> P0-0类修复：M5 ScaleHead零梯度 → 解析守恒缩放替代。零GPU（post-hoc，final-100 preds复用）。

## 结论：盲缩达到oracle水平，机制验证通过

| 口径 | phDOS med/fail |
|---|---|
| oracle（真值sum反归一） | 0.833 / 2.5% |
| blind-3N（无真值，3N解析缩放） | 0.815 / 5.2% |
| blind-3N×0.92（截尾校准） | **0.839 / 3.4%** |

- 公式：`Y_i = d_i × 3N/Δ`（d为softmax/自归一形状，Δ bin宽；注意不是3N/(BΔ)——
  第一版错除B=128，R²干到-0.04，量纲检查抓回）。
- 0.92是数据冻结常数（P2窗截尾+MP归一惯例，面积/3N中位0.917，A2b式校准）。
- P0-0关闭：声子盲推不再需要学习scale，也不需真值min/max——M5的ScaleHead可以删了（E阶段动手）。

## eDOS守恒：有理由挂起

价电子标定要求全谱积分；E0窗[-6,6]切掉深能级态，窗内面积不是守恒量。
要做需全谱eDOS臂（另起网格项，不在C2.2内）。JARVIS-eDOS继续只验证。

## 接线

推理侧（cif2dos/test_cif盲模式）按`N=掩膜有效原子数 → scale=3N×0.92/Δ`实现，
属E阶段部署活，不占GPU，在此只冻公式与常数。
