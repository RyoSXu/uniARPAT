# 工作日志 2026-09-12：A7c谱拉取收官

## 改了什么
- 新增 `tools/getdata/a7c_fetch_jarvis.py`（复用pilot的`jarvis.db.webpages`路径，
  一页serving两种谱；断点续跑+jvasp key+6线程；缺失记None+errors永不丢行）；
- 落盘：`jarvis_threesrc_raw.jsonl`（3,601条，8.43GB，eDOS+phDOS）、
  `jarvis_gap_raw.jsonl`（28,852条，6.31GB，phDOS）、`jarvis_gap_hit_canon.json`。

## 结果如何
- 三源子集：3,601全回；eDOS 3,599（99.9%）、phDOS 2,102（58.4%）→ A2b三源矩阵输入齐了。
- 缺口：28,852全回；phDOS命中3,768（13.1%）→ 3,600唯一canonical（尾部新区段覆盖稀，符合预期）。
- **并池**：PhononDB增量4,391 ∪ JARVIS增量3,600，重叠仅267（两源互补性好）→
  新增双谱**7,724**，真双谱 18,644 → **26,368（+41.4%）**。
- 耗时：翻译~4h API + 谱拉取~2.5h webpage；教训：整批查询在死ID分散时零命中，直接singles。

## 下一步
A2b三极gap（输入全齐：MP谱本地 + PhononDB复算本地 + JARVIS三源子集本地）→ 源权重 → A4加工。
配对铁律重申：JVASP结构配JVASP谱+源标签。
