# 日志-2026-09-18-E9P0-Q2实现与pilot

## scope决议（先记结论）
- held-out query**训练**须loss内bin掩膜 → 撞冻结#10（掩膜永留评估端，C2.3死刑）→ **Q2不做**；
  合法载具是Q3连续读出（E9-R&D方向）或未来warp稠标签（E9后队列）。Q2 verdict=编码因子。
- Design“+Softplus非负”不进trunk（破零初值归因）：读出非负已有`safe_shape_norm`；
  Softplus读出归Q3（`y=Softplus(<b,t>+c)`）。

## 改了什么（默认off，Q1通路零改动）
- `model/heads.py`：`FourierTrunk`（ frozen RFF bank seed固定 + `[x,sin,cos(2πBx)]` → MLP → 零初值proj；
  eDOS 64频σ8高频 / phDOS 32频σ2低频；推理clip训练域，冻网格下no-op）。
- `model/transformer.py`：`q2_fourier`开关（蕴含trunk通路；Q2-vs-Q1exp即Fourier单因子）；
  `run_ablation`加`--q2_fourier`透传+config记录。
- 单测`tests/test_q2_fourier.py`4项：bank确定性（精确重算）、任务sigma分离、零沉默+clip+梯度、
  开关接线+权重移植day-0等价。

## 实测
- 全套31/31过；CPU冒烟（13 loss键）；参数71,310,148（+157k vs base，可归因量级）。

## pilot verdict（2026-09-18 08:58落盘，M1×10，`_q2exp` vs 盘上双对照）
- test：ctl e **0.475/4.50** p **0.722/1.22**；q1exp e 0.469/4.33 p 0.717/1.66；
  q2exp e **0.472/5.03** p **0.715/1.66**。
  Fourier因子（q2−q1exp）：med +0.003/−0.002，fail +0.70pt/持平；
  total（q2−ctl）：med −0.003/−0.007，fail +0.52/+0.44pt。
- valid ep10：q2 e 0.466/4.84，q1exp 0.464/4.63，ctl 0.462/4.63（全差<0.005）。
- 判决：**全面打平（<0.02线），Q2 park，Q组关闭**（Design-E§5输停；held-out训练本就移交Q3）。
  Q1/Q2代码+9单测留存默认off。E9-P0 encoder/query侧至此无win：
  G1挂起→Q1 park→Q2 park（与C1/M3“换架构/表示零转译”规律一致）。
