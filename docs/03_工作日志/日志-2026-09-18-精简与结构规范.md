# 日志-2026-09-18-精简与结构规范

## 改了什么（减法为主，加法只加索引）
- 删（ignored+可再生，零风险）：`ablation_train.log`（353KB旧日志）、全部 `__pycache__/`、`results/pred_*.npy` 94个（`results/` 117M→23M，可由checkpoint+runner重算；`history/test/samples` csv全留，verdict证据不动）。
- 归档（`git mv`→`tools/legacy/`+README，执行Backlog E8，历史可恢复）：`train.py`/`test.py`/`train_script.sh`/`test_script.sh`（旧链，引用的`data/csv2npy.py`已不存在）、`run_pilot_10epochs.py`（硬编码已作废M5 ScaleHead）、`test_cif.py`（读不存在的v1路径，E8留一胜者为`cif2dos.py`）、`evaluate_and_plot.py`（读旧布局`output/config/.../training_options.yaml`）。根目录现仅3个py：`run_ablation_experiments.py`/`cif2dos.py`/`thermo_props.py`。
- 登记（不动文件）：`tools/getdata/README.md` 补 `q1_rebuild/c2b_grids/recompute_phonondb/download_phonondb/extract_*/fetch_structures/dump_atom_features` 行 + `process.py` 标旧稿；`docs/04_前沿探索/README.md` 补跨学科行。
- `.gitignore`：加 `data/archive/`（本地GB缓存止刷屏，文件留磁盘）+ `data/train4ARPAT/`（仅manifest/stats入库）。
- 同步：Backlog E8标[x]；README结构树加legacy行+根3py注；STATUS Watch更新。

## 不碰的（需总指挥逐项批）
- `output/` 约74G checkpoints：删任一不可逆（GPU重跑成本），本次只定政策不动文件。
- `results/` tracked csv（132个）、`figures/` 5图、`docs/04` 10篇、`tools/getdata` 30脚本：只加索引不删。

## 实测
- `python3 -c "import cif2dos"` OK；`run_ablation_experiments.py --help` OK。
- 现行文档零引用旧入口（grep README/AGENTS/STATUS/INDEX/GLOSSARY/README/tools/eval零命中）；历史archive/日志引用冻结保留。
- `python3 -m unittest discover tests`：34/34 OK。
