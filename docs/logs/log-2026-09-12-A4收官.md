# 工作日志 2026-09-12：A4/A6/A5收官（v2数据集落盘，可训）

## 改了什么
- 新增 `tools/getdata/a4_process.py`（pass1：网格重切+原胞+源标签+82格式）、
  `a4_pass2.py`（winsorize+终盘+验收）、`a6_split.py`（分层切分）、
  `build_v2_cache.py`（npy缓存+manifest+--check）；
- 落盘：`v2_processed.parquet`（24,988行，含split列）、`data/train4ARPAT-v2/`（20,040/2,477/2,471）、
  `stats_v2.json`、`v2_outlier_attribution.csv`、`v2_audit_alt.csv`（6,862外源备视图）、
  `index/split_v2.yaml`、`probeanel.json`。

## 口径定板（Si标尺+源码考据）
1. 声子密度除以33.356（密度守恒，Si 0.0602 vs v1 0.0605对上；之前乘反33倍，A2b因面积归一未受影响）。
2. eDOS与v1差异（Si相关0.40）是legacy-task漂移，A4按血缘规则走，差值记B2的账。
3. 缓存保持82格式（哨兵126/127+1/c），loader+Transformer前向冒烟通过（1,128/1,64）。
4. atom_feat当前是3维map（`atom_feature_map`实测119×3，注释27是旧数）；
   A5合同的[80,24]是C1目标，Parquet照3维存，命名归C1/A9清理。
5. 主视图原胞化导致标签尺度系统性小于v1（edos中位max 15.1 vs 19.5），B2重标定须注明不可直比。

## 验收
- pass1：24,988行（MP 18,644 + PhononDB 4,049 + JARVIS 2,295；1,014无可用声子出池；截断324）。
- pass2：clip 1,229单bin伪影，宽峰511保留；CIF round-trip 1000/1000。
- A6：成分零跨集断言通过；三层比例±2%内；探针1条；He规则跳过（v2无He）。
- A5：--check经训练Dataset，forward冒烟ok；掩膜sidecar已存（C2消费）。
- 坑：增量分支down=None守卫缺失致3,932行NaN（已修）；JARVIS 1,035纯弹性记录无DOS；
  unresolved条目identifier残留键（A4 mode过滤无影响，A9注明）。
