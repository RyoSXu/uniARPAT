# Design-A6：分层切分v2

**状态**：已冻结，待A4输出即执行 ｜ **规范**：[数据加工规范DataSpec.md](./数据加工规范DataSpec.md)

## 决议（2026-09-10讨论锁定）
- 比例8:1:1（与v1可比）；seed42；旧索引保留。
- 三层分层：金属/半导体/绝缘体（带隙0/0–2/>2eV）＋ 7晶系（稀有晶系保底进训练+记录）＋ 峰均比难度三分位。
- 同成分硬隔离（v1重叠13.6%教训；同成分对18.5%相关<0.5，隔离后fail率才是真泛化）。
- He只进训练集；测试集保留"未见元素探针"单列。
- 输出`index/split_v2.yaml`（三集id+seed+代码hash）。
- 验收：各层比例三集一致±2%；成分零跨集（脚本断言）；口径变化汇报注明（绝对分不与v1直比）。
