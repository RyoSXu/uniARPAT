# 日志-2026-09-18-文档精简第二轮

## 改了什么
- Design 完成即归档（`git mv`→`01_开发文档/archive/`，执行该目录 README 既有规则）：
  A3census / A4v2 / A6 / B2（对应任务均为[x]收官）。现行 Design 8→4，只剩 A7（L2待议未完）+ C5/E/P0（未来工作）。
- 链路同步：Backlog A3/B2 两处链接加 `archive/` 前缀；A6 日志 1 处设计路径修为 archive 路径（纯链路修复，内容未动）；
  现行文档 grep 零断链（历史日志冻结保留bare名引用，可搜到）。
- 死代码：`git rm utils/rbf_encoding.py`（全仓零导入；RBF 数学沿用 `rp_encoding.py`；单测 `test_p0_fixes` 本就断言其不在层上）。
- 不删的（逐项有理由）：工作日志 30 篇（审计链，append-only）、`04_前沿探索` 11 篇（Roadmap 输入证据，已做命运登记；删知识省几十KB丢证据链，不值）、
  `fig_pilot_10epochs.png`（archive验收报告引用）、`pilot_10epochs_metrics.csv`（同上）、`data/archive/v1`（40MB本地只读源+已ignore）、
  `split_v2.yaml`（a6/DataSpec引用）、`output/` checkpoints（74G，逐项批政策不变）。

## 实测
- `python3 -m unittest discover tests`：34/34 OK（含 `test_p0_fixes` 死模块断言）。
- `grep rbf_encoding --include=*.py` 全仓零命中。
