# 工作日志 2026-09-12：Top-up双缺口收官 + 结构选型转正

## 改了什么
- 新增 `tools/getdata/top1_mp_increment.py`（增量集summary+es批量，7,724全回零unresolved）、
  `tools/getdata/delta_edos_index.py`（Delta eDOS归属索引691,978行，37秒建成，
  `delta_edos_index.parquet`可复用）、`tools/getdata/dosmap_final.py`（终判）、
  `tools/getdata/resume_dl.py`（断点续传通用）；
- 落盘：`mp_increment_raw.jsonl`、`mp_increment_dosmap.json`、
  `jdft_3d-12-12-2022.json.zip`（40M，75,993条，含atoms）、`delta_edos_index.parquet`。

## 关键发现（已入库）
1. **ES口径坑**：`/materials/electronic_structure/`的dos指针只覆盖67%，
   而dos-route总量271,321证明谱都在Delta里——迁移后REST不再暴露material→task映射。
   解法：本地Delta结构归属（nsites+物种+dV三级+matcher终判）。
2. **MP-eDOS落实**：REST直给5,195 + Delta归属2,163；241 Delta真无 + 129模糊出池；
   增量MP-eDOS：PhononDB 4,049/4,391、JARVIS 3,572/3,600；
   **谱落实双谱池：新增7,358，真双谱18,644→26,002（+39.5%）**（ID口径26,368作废）。
3. **结构选型转正**：JVASP vs MP（n=5,870全量，dft_3d自带spg号）dV/atom中位**1.7%**、
   空间群一致**98.0%**；PhononDB侧2.7%/98.6% → **A4输入统一用MP结构+源标签**，关闭选型。
   （教训：MP summary lattice是dict，PhononDB是裸matrix，混用致matcher 0/731，修后606/731。）
4. JARVIS结构不用逐页抓：`Webpage`无`.structure`且抖动大，figshare整包+Range续传一次解决，
   命中5,870全覆盖（2022快照够用）。

## 下一步
A4加工（Design-A4）：输入=MP双谱18,644 + PhononDB增量4,049 + JARVIS增量3,572（谱落实口径），
权重按A2b（PhononDB≈1校准 / JARVIS降权+源标签+强度重归一），结构统一MP+源标签。
