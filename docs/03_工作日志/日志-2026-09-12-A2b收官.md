# 工作日志 2026-09-12：A2b三源gap收官

## 改了什么
- 新增 `tools/getdata/a2b_gap.py`（全本地：公共交集网格2cm⁻¹/0.1eV，禁外推；
  面积归一Pearson r + 互相关峰位移 + 面积比；eDOS以MP Fermi为锚、JARVIS±15eV配准；
  MP声子优先pheasy；结构delta MP vs PhononDB）；
- 落盘：`a2b_pairs_phdos.csv`（1,960对）、`a2b_pairs_edos.csv`（3,352对）、`a2b_summary.json`。
- 坑位修复：numpy无`trapezoid`用`trapz`；JARVIS字段是逗号字符串需split+去引号；
  133条纯弹性记录无DOS键跳过；MP单自旋down=null按总量处理。

## 结果如何（三源分歧矩阵，n=1,960声子对 / 3,352电子对）
- 声子MP-PhononDB：r中位**0.893**（p10 0.70），lag中位0，面积比1.00 → **互换+轻校准**。
- 声子MP-JARVIS：r中位**0.658**，lag中位2cm⁻¹ → **源标签加权**；
  另发现JARVIS声子强度为任意单位（面积比0.03），A4前必须按3N模数求和规则重归一、只用形状。
- PhononDB-JARVIS：0.657，同上处理。
- 电子MP-JARVIS：配准后r中位**0.79**（47%>0.8）；位移94%落在+0.5/+1.0eV，
  4样本r(sh)曲线验证为两源绝对能量零点系统差（r 0.3→0.9），非配准伪影；
  A2的0.54即未配准低估。JARVIS-eDOS只验证、不进训练。
- 结构MPvsPhononDB（n=1,935）：dV/atom中位2.7%，空间群一致率**98.6%** →
  增量双谱暂定**MP结构作训练输入+源标签**，JVASP结构delta待Top-up#2复核。

## 下一步
Top-up双缺口（MP增量结构+eDOS映射；JARVIS命中结构二遍）→ A4加工（权重：PhononDB≈1.0校准混入，
JARVIS声子降权+源标签；JARVIS强度重归一）。
