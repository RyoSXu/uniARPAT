# Design-A4：v2加工执行（含A5落盘，2026-09-10冻结）

**状态**：已冻结，待A3构成表即执行 ｜ **规范**：[数据加工规范DataSpec.md](./数据加工规范DataSpec.md)

## A5落盘契约（2026-09-10锁定）
- Parquet列：mpid/jid/split/elements[80]/positions[80,3]/lattice[9]/sg/occupancy[80]/atom_feat[80,24]/edos[128]/phdos[64]/raw_ref/prov。
- atom_feat存全量（240MB换零计算）；谱存raw_ref指回（省3GB）；float32全场。
- `build_v2_cache.py --parquet --split --grids --out [--check]`；输出npy+stats_v2.json+manifest+bucket_idx.json。
- 验收：行数一致；抽1000 diff<1e-6；试加载前向冒烟。

## 要点
- 接口骨架：`tools/getdata/process.py`（待填CAP_ATOMS/GRID）→ Parquet全集 → `build_v2_cache.py` → npy缓存。
- A5决议（2026-09-10）：训练缓存新起`train4ARPAT-v2/`目录，与v1并存（回滚零成本）；
  CAP_ATOMS不定死80，按抽取原子数分布p99.9定（候选96/128），超cap截断+警告+prov记录。
- 验收：round-trip抽1000 diff=0；视图相关>0.99；离群归因表清零。
