# 日志-2026-09-17-E9P0-Q1实现与pilot

## 改了什么（E9-P0 Q1，默认off，off-path零改动）
- 网格核对：E0边`linspace(-6,6,129)`/宽0.09375（=delta_edos，Fermi=0为边）；
  P0边`linspace(-280,980,65)`/宽19.6875（=delta_phdos，nu=0在bin14内）；坐标=bin中心即任务零点系。
- `model/heads.py`：`CoordTrunk`（Linear1→128→GELU→零初值Linear→512，双任务独立，零保持归一/6与/980）。
- `model/transformer.py`：`q1_coord/q1_hidden`开关 + 双trunk（_reset后重零）；
  forward加`edos_x/phdos_x`可选参（缺/错bin数断言大叫）；query残差注入（Q0静态query不动）。
- `datasets/dataset.py`：`__getitem__`追加[15]edos_x/[16]phdos_x（常量克隆，源自`grids.json` E0/P0，
  bin数断言；坐标是网格常量非标签，无泄漏）；`model.py`解包+两处前向透传；
  `run_ablation/evaluate/pilot`三处评估入口同步；runner加`--q1_coord/--q1_hidden`。
- 与C1.2分工：C1.2（eDOS-only Fourier+RFF）维持parked；Q1是E9-track双任务 plain-MLP版（Fourier归Q2）。
- 单测`tests/test_q1_coord.py`5项：坐标=E0/P0中心+零点约定、trunk零输出+权重移植day-0等价
  （注：开关两模型RNG流天然不同，C1.2同理，保证的是per-model残差沉默）、trunk可学、
  缺x/错bin断言、17项batch与legacy兼容。

## 实测
- 全套27/27过；真数据CPU单步冒烟（17项batch，x [4,128]/[4,64]，loss全键，6.5s/step）。

## pilot verdict（2026-09-17 23:17双臂落盘，M1×10，同池同seed同配方）
- test（best ckpt）：ctl eDOS **0.475/4.50%**、phDOS **0.722/1.22%**；
  exp eDOS **0.469/4.33%**、phDOS **0.717/1.66%**
  → med -0.006/-0.005，fail -0.17pt/+0.44pt。
- valid ep10：ctl e 0.462/4.63、p 0.720/2.03；exp e 0.464/4.63、p 0.721/2.08 → 全差<0.005。
- 成本：190s/ep双臂一致，显存7150 vs 7145MB（+5MB噪声）→ 成本中性。
- 判决：**全面打平，按C1.1先例park（默认off，代码+单测留存），不合并**。
  合并纪律：合并须有win（参考H1盲gap大胜/C2.1+0.06），draw只留路。
  Q2（Fourier）直接 حمل trunk一起做臂，对`_q1exp`裁Fourier单因子、对`_q1ctl`看total；
  峰区/带隙区/held-out query等泛化 verdict 归Q2（本轮samples只有样本级全局量，bin级zone暂缺）。
