# 工作日志 2026-09-11：h1收官（M1–M4）+ 进程管理教训

## h1最终成绩（test集，seed42，干净对照）

| | eDOS med/fail | phDOS med/fail |
|---|---|---|
| M1 | 0.519/12.3% (best ep43) | 0.678/10.0% |
| M2 | 0.526/13.3% (best ep38) | 0.681/8.5% |
| M3 | 0.491/16.2% | 0.650/11.6% |
| M4 | 0.481/17.4% (best ep69) | 0.649/12.2% |

## 三刀判决
1. 解耦（M2−M1）：≈零，v2砍回共享解码器（省~35M）。
2. 对称头（M3−M2）：负的，eDOS头回退轻量，预算转能量编码+RFF。
3. 门控（M4−M3）：负的，判死刑，预算转MoE。
- M5未跑（scale零梯度待P0-0修复）。

## 进程管理事故（M2–M4连死）
- 现象：后台GPU任务无声死亡、无traceback；误杀正常单例两次（把worker/正常进程当双跑杀了）。
- 对策已落地：断点续跑（验证生效）+ 原子保存 + flock单实例 + GPU占用者检查（ps看PPID+`nvidia-smi --query-compute-apps`，不只看进程名）。
- M4实例：跑到ep99死亡，resume 5分钟收尾——resume机制的价值实证。

## 输出归档
`results/*_{m1,m2,m3,m4}_h1.*`，v0文件从git恢复。
