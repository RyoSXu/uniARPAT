# Design-A4：v2加工执行

**状态**：讨论中（等A3构成表；A5决议已并入本件） ｜ **规范**：[数据加工规范DataSpec.md](./数据加工规范DataSpec.md)

## 要点
- 接口骨架：`getdata/v2/process.py`（待填CAP_ATOMS/GRID）→ Parquet全集 → `build_v2_cache.py` → npy缓存。
- A5决议（2026-09-10）：训练缓存新起`train4ARPAT-v2/`目录，与v1并存（回滚零成本）；
  CAP_ATOMS不定死80，按抽取原子数分布p99.9定（候选96/128），超cap截断+警告+prov记录。
- 验收：round-trip抽1000 diff=0；视图相关>0.99；离群归因表清零。
