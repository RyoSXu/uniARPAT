# 日志-2026-09-17-E9-P0-G1实现与pilot

## 改了什么（E9-P0 G1，默认off，off-path零改动）
- 新增 `utils/g1_graph.py`：`build_g1_graph(pos, mask_atom, r_cut=5.5, max_neighbors=48, t_range=2)`
  + `quintic_cutoff`（C2连续、值域钳[0,1]）。
  - 全枚举 `T∈[-2,2]³`（125定向，25-chunk广播实现）取Cartesian最小，替代`diff_frac-round()`单镜像；
    自对 `(0)`、pad行回退到全valid、度数封顶48+self、strict`<r_cut`（边界=阻断，连续选择）。
- `model/transformer.py`：`Transformer(use_g1/g1_r_cut/g1_max_neighbors/g1_t_range)` + 零初值hub token；
  forward G1分支（图→hub扩展L+1→encoder→剥离hub，decoder/记忆体合同不变）；
  `TransformerEncoder/EncoderLayer`加可选`g1_adj/g1_smooth`（smooth乘RP，adj与padding掩膜取并集-1e9）。
- `run_ablation_experiments.py`：`--use_g1/--g1_r_cut/--g1_max_neighbors`透传+`config_used.yaml`记录。
- 新增 `tests/test_g1_graph.py`：枚举修正+旋转/平移/置换/镜像/基变换五门+cutoff/topk/pad合同，共7项。

## 关键实测（证据）
- 偏斜小胞（a=2.89/b=3.86/c=11.50/47.8°）：旧4.0→真2.65（纠偏1.3Å），模块=暴力最小（1e-3）。
- 全套 `python3 -m unittest discover tests`：**22/22过**（旧15+新7）。
- 真数据CPU单步冒烟：loss全键（含`loss_eta`，H1与G1共存），5.4s/step@batch4。
- e3nn零向量安全（`[1,0,…]`）；float32边界相消已钳（此前-4.8e-7负权）。

## pilot（进行中，V100）
- `setsid nohup python3 run_ablation_experiments.py --model M1 --epochs 10 --tag _g1pilot --use_g1`
  （pid 23693，Q1池+生产配方，~4min/ep，预计~1h；71.15M params，8.75GB）。
- verdict待落盘对照B7（eDOS 0.518/5.73%，phDOS 0.741/3.50%）：判据median+fail+盲gap（沿用H1脚本口径）；
  通过→G1转正（默认on）进Q组，不通过→按输停（Design-E§5）退回legacy。

## pilot verdict（2026-09-17 15:21落盘，10轮，同池Q1）
- test（best ckpt）：G1 eDOS **0.466/4.94%**、phDOS **0.713/1.84%**，Cv 0.31。
- 同轮valid对B7-ep10（`history_m1_e9ctl.csv`）：G1 e 0.464/4.28 vs B7 e 0.454/4.80；
  G1 p 0.716/2.29 vs B7 p 0.703/2.25 → **med双优（+0.010/+0.013），fail电子-0.5pt**。
- 成本：196s/ep（+3.7%）、峰值7228MB（+1.2%）——**显存未降量级**（dense实现下符合预期，
  Design-E“显存降量级”判项记未达成，精度项达成+五门全过）。
- 判决：**机制验证通过、微优，晋级35轮对齐**（`_g1long` vs B7-35 head-to-head，含盲gap重验）；
  不是转正（pilot尺度+收敛中best=ep10，须长跑确认）。

## _g1long verdict（2026-09-17 17:54落盘，M1×35，best ep31，无崩塌）
- test head-to-head（同池Q1同配方）：
  G1 eDOS **0.514/5.64%** vs B7 0.518/5.73%（-0.004/-0.09pt）；
  G1 phDOS **0.733/3.50%** vs B7 0.741/3.50%（-0.008/持平）；Cv 0.30=0.30。
- 盲gap（`test_m1_g1long_blind_summary.csv`，oracle复现4位小数）：
  电子盲0.477 vs 0.480（gap p50 0.0105 vs 0.010，p99 0.82 vs 0.83）；
  声子盲0.725 vs 0.735（gap p50 0.0009 vs 0.0008，p99 0.51 vs 0.56）；η/γ MAE 0.042/0.067≈B7。
- 零训练归因切片（2287逐样本oracle对位，G1−B7）：
  skew>15（n=1656）：-0.004/-0.005；skew>25（n=1498）：-0.003/-0.004；
  minabc<3（n=57）：+0.006/-0.048（噪声口径，phDOS负向记watch-item）。
  → **无条件胜，枚举修正的正确性价值在M1尺度零转译**（与M3探针“瓶颈不在几何精度”收敛）。
- 判决：**G1挂起（park），默认off**——代码+7单测留存，`_g1long` ckpt作Q/C5备选骨干；
  不合并（+3.7%时间/+1.2%显存零涨点，hub vs 稀疏 unattributed，合并不符合单因子）。
  E9-P0下一棒：**Q1坐标MLP**（固定encoder，Design-E§9，Q1→Q2输停）。
